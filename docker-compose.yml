version: '3.8'

services:
  # ベクトルデータベース (Qdrant)
  vector-db:
    image: qdrant/qdrant:latest
    container_name: qdrant-db
    ports:
      - "6333:6333" # API用ポート
    volumes:
      - ./qdrant_data:/qdrant/storage # データを永続化
    restart: always